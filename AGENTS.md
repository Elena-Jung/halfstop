# AGENTS.md

이 저장소에서 일하는 에이전트를 위한 안내입니다. 규칙마다 왜 그런지를 함께 적었습니다.
이유를 알면 여기 안 적힌 상황에서도 같은 방향으로 판단할 수 있기 때문입니다.

<project>

## 무엇을 만들고 있는지

halfstop 은 사진의 EXIF 를 읽어 촬영 정보 프레임을 붙이고 다시 내려받는 웹앱입니다.
카메라와 렌즈 이름, 조리개, 셔터 속도, ISO, 초점 거리 같은 값을 사진 아래나 사방 여백에
얹습니다.

**핵심 제약은 이미지가 브라우저를 떠나지 않는 것입니다.** 업로드 경로가 아예 없으므로
약속이 아니라 구조로 보장됩니다. 서버 코드가 없고 정적 파일만 배포합니다. 이 제약을 깨는
변경은 이 프로젝트의 존재 이유를 지웁니다. 네트워크 요청을 만드는 코드를 넣지 마십시오.

부가 목적은 **앱의 반응 속도**입니다. 사용자가 슬라이더를 조금씩 움직이며 결과를 보는
도구라, 미리보기가 늦으면 만지는 뜻이 줄어듭니다. 개발 속도가 아니라 앱의 반응 속도입니다.

공개 배포이므로 **민감정보가 없는 것을 목표로 합니다.** 프레임에 그릴 수 있는 값은 촬영
설정과 장비명뿐입니다. GPS, 소유자명, 일련번호는 읽지도 그리지도 않습니다. 저장소에 API
토큰이나 계정 식별자를 넣지 마십시오.

## 어디까지 왔고 어디로 가는지

| 단계 | 내용 | 상태 |
|---|---|---|
| 1 | 렌더 파이프라인. EXIF 읽기, 디코딩, 방향 정규화, layout/paint 분리, 캔버스 한계 측정, 내보내기, Web Worker | 완료, main 에 병합 |
| 2 | 레이아웃과 프리셋. 텍스트 슬롯, 하단 바, 4면 여백 액자, 프리셋 아홉 개, 설정 저장 | 완료, main 에 병합 |
| 3-1 | 배포. Cloudflare Pages, 검사 워크플로, 소개 문구와 검색 최적화 | 완료, main 에 병합 |
| 3-2 | 화면 본체. 아이콘 레일과 사이드 패널, 한국어 사전, 어두운 테마, 접근성 | 진행 중, 브랜치 `ui-shell` |
| 3-3 | 브랜드 로고, 촬영 정보가 없는 사진의 확인 창 | 아직 계획 없음 |

아직 없는 기능을 있다고 쓰지 마십시오. HEIC 와 RAW 열기, 여러 장 한 번에 처리와 ZIP,
브랜드 로고 표시, EXIF 손편집은 설계 문서에 있지만 **구현되어 있지 않습니다.** 사용자에게
보이는 글(README, 화면 문구, 소개 내용)에 기능을 적을 때는 그것을 하는 코드 파일을 댈 수
있어야 합니다. 댈 수 없으면 쓰지 마십시오.

</project>

<architecture>

## 반드시 지켜야 하는 세 가지

이 셋이 이 코드베이스의 뼈대입니다. 나머지는 대부분 취향 문제지만 이것들은 아닙니다.

### 1. 크기 단위가 픽셀이 아닙니다

`1u = 사진 짧은 변 / 1000` 입니다. 옵션에서 크기를 뜻하는 값은 모두 디자인 단위입니다.

**왜.** 픽셀로 두면 같은 설정이 1200만 화소 사진과 4500만 화소 사진에서 다른 결과를 냅니다.
이 정규화가 그 문제를 자료 구조 수준에서 없앱니다.

### 2. `layout()` 과 `paint()` 가 나뉘어 있습니다

`layout()` 은 순수 함수로 불변 `Scene` 트리만 만듭니다. 캔버스를 만지지 않습니다.
공용 `paint()` 가 `ctx.scale(pxPerUnit, pxPerUnit)` 을 **딱 한 번** 적용하고 디자인 단위로
그립니다. 미리보기와 내보내기는 `pxPerUnit` 만 다른 같은 호출입니다.

**왜.** 이 앱에서 가장 비싼 버그는 미리보기와 결과물이 갈라지는 것입니다. 레이아웃을 두 번
작성하면 반드시 어긋납니다. 이 구조가 그것을 막는 유일한 장치입니다.

말줄임과 글자 폭 측정은 메인 스레드에서 끝나고, 워커에는 **이미 잘린 문자열**이 `Scene` 안에
실려 갑니다. 워커가 서체를 따로 등록해도 배치가 흔들릴 수 없습니다. 배율을 한 번만 적용하는
것보다 이 결정이 더 강한 보장입니다. 이 성질을 깨지 마십시오.

`src/core/paint/paint.test.ts` 가 배율 1 과 0.25 의 명령 기록이 최상위 `scale()` 하나를 빼고
같은지 검사합니다. 이 테스트가 실패하면 추상화가 샌 것입니다.

### 3. 프리셋은 코드가 아니라 데이터입니다

레이아웃 함수는 `barLayout` 과 `matteLayout` 둘뿐입니다. 프리셋 아홉 개는 그 위에 얹힌 옵션
값 묶음입니다. 값은 **선언 기본값 → 프리셋 값 → 저장된 값** 세 겹으로 쌓이고
`mergeValues` 의 `base` 인자가 그 순서를 지킵니다.

**왜.** 프리셋을 늘리거나 버리는 데 코드가 들지 않고, 사용자가 만든 변형도 같은 모양으로
저장됩니다. 레일 구성도 `groupKey` 라는 데이터에서 나옵니다.

**데이터의 오류는 조용합니다.** 선언에 없는 키는 예외 없이 무시되고, 범위를 벗어난 값은
`coerce` 가 말없이 잘라냅니다. 프리셋이나 옵션을 더할 때는 키를 선언과 대조하는 테스트가
있는지 확인하십시오.

## 모듈 경계

```
src/core/      React 와 DOM 에 의존하지 않는 영역
src/platform/  localStorage 를 만지는 유일한 자리
src/worker/    전체 해상도 렌더링
src/i18n/      한국어 사전과 t()
src/ui/        React 화면
```

`src/core/` 아래 코드는 `react` 를 import 하지 않고 `document`, `window`, `localStorage`,
`fetch` 를 참조하지 않습니다.

**왜.** 전체 해상도 내보내기가 Web Worker 에서 돌기 때문입니다. 한 곳이라도 새면 워커
내보내기가 **예외 없이 조용히** 죽습니다.

`tsconfig.json` 이 프로젝트 전체에 DOM 라이브러리를 넣어 두어 `tsc` 는 이 경계를 지켜 주지
않습니다. `src/core/coreBoundary.test.ts` 가 그 자리를 대신합니다. 이 테스트를 약하게
만들지 마십시오.

`core` 는 **번역된 문자열을 반환하지 않습니다.** 키와 구조화된 값만 반환하고 UI 가 옮깁니다.
`src/ui/errorMessage.ts` 가 그 경계이고, `core` 가 던진 `Error.message` 를 정규식으로
분류해 번역 키로 바꿉니다.

</architecture>

<documents>

## 저장소에 있는 문서

| 경로 | 무엇인지 |
|---|---|
| `README.md` | 사람이 읽는 소개. 개발, 테스트, 빌드, 컨테이너 확인, 배포 방법 |
| `AGENTS.md` | 이 파일 |
| `docs/css-style.md` | CSS 규약. 색과 치수 토큰, 어느 단계를 어디에, 감추기와 초점, 눈으로 확인할 것 |
| `docs/superpowers/specs/2026-09-09-halfstop-design.md` | 1단계 설계. 렌더링 구조, 브라우저 한계 대응, 브랜드 정규화, 모듈 구조 |
| `docs/superpowers/specs/2026-09-09-halfstop-ui-design.md` | 2단계 설계. 프리셋 아홉 개, 레일과 패널, 접근성, i18n, 테마 |
| `docs/superpowers/plans/2026-09-09-halfstop-render-pipeline.md` | 1단계 구현 계획, 19개 작업 |
| `docs/superpowers/plans/2026-09-09-halfstop-presets.md` | 2단계 구현 계획, 6개 작업 |
| `docs/superpowers/plans/2026-09-10-halfstop-ui-shell.md` | 화면 본체 구현 계획, 6개 작업 |

**스펙과 계획의 관계.** 스펙은 무엇을 왜 만드는지를 정합니다. 계획은 그것을 작업 단위로
쪼개고 각 단계에 실제 코드와 검사 명령을 적습니다. **둘이 어긋나면 스펙이 이깁니다.** 스펙은
사용자가 승인한 문서이고 계획은 그것을 옮긴 것입니다.

계획서에 적힌 코드가 틀렸다고 판단되면 **그대로 옮기지 말고 멈추고 보고하십시오.** 이
저장소에서 실제로 계획서가 산술 오류를 담은 적이 세 번 있고, 그것을 의심 없이 옮긴 탓에 두
번 더 고쳐야 했습니다. 지시를 의심해서 보고한 것이 가장 값진 행동이었습니다.

## 앞으로 생길 문서

- 3-3 단계 스펙과 계획. 브랜드 로고와 촬영 정보가 없는 사진의 확인 창입니다. 로고는 상표
  문제가 있어 레지스트리를 데이터 주도로 만들어 나중에 교체하거나 뺄 여지를 남겨야 합니다.
- 영어 사전 `src/i18n/en.ts`. 지금은 한국어만 채워져 있고 구조는 서 있습니다.

## 작업 기록

`.superpowers/sdd/` 는 git 이 무시하는 작업용 디렉터리입니다. 계획 실행 중의 지시서, 보고서,
리뷰용 diff 가 쌓입니다. 그 안의 `progress.md` 가 **복구용 대장**입니다. 문맥이 날아가면
대화 기억보다 이 파일과 `git log` 를 믿으십시오.

</documents>

<conventions>

## 한국어

**모든 한국어는 습니다체입니다.** 화면에 보이는 문구, 코드 주석, 커밋 메시지, 문서 모두
해당합니다. 해요체를 쓰지 않습니다.

하십시오체(`고르십시오`)는 습니다체와 같은 합쇼체 층위의 명령형이므로 **위반이 아닙니다.**
리뷰에서 이것을 위반으로 잡아 오면 반려하십시오.

`src/i18n/index.test.ts` 가 사전의 모든 값에 대해 해요체 종결 어미를 검사합니다.

em-dash(U+2014)와 en-dash(U+2013)를 쓰지 않습니다. 공백 하이픈, 쉼표, 콜론으로 대신합니다.

**여러 뜻으로 읽히는 동사를 화면 문구에 쓰지 마십시오.** 무슨 일이 일어났는지 사실로 풀어
씁니다.

<example>
나쁨: `나머지 2장은 넘겼습니다`
왜: 건너뛴 것인지 처리한 것인지 구분되지 않습니다. 사용자가 실제로 이해하지 못했습니다.
좋음: `{name} 파일 하나만 불러왔습니다. 여러 장을 한 번에 처리하는 기능은 아직 없습니다`
</example>

화면 문구를 새로 하드코딩하지 마십시오. 전부 `t()` 로 갑니다. 새 문구가 필요하면
`src/i18n/ko.ts` 에 키를 더하십시오.

## 커밋

제목은 명사구로 씁니다. 본문에는 **무엇을 했는지보다 왜 그렇게 했는지**를 적으십시오.
무엇을 했는지는 diff 가 이미 말합니다.

마지막 줄에 `Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>` 를 넣습니다.

**`git add -A` 를 쓰지 마십시오.** 반드시 파일 경로를 지정하십시오. 문서만 담을 커밋에 다른
작업자의 작업 중이던 파일이 딸려 들어간 적이 있습니다. 여러 에이전트가 동시에 돌 때 특히
위험합니다.

## 의존성

새 의존성을 추가하지 마십시오. 지금 있는 것은 `exifreader`, `react`, `react-dom`,
`lucide-react`, 그리고 서체 네 개뿐입니다.

예외는 **스펙이 이름을 대고 근거를 적어 둔 경우**뿐입니다. `lucide-react` 가 그렇게
들어왔습니다. 스펙에 없는 것을 넣으려면 먼저 물어보십시오.

특히 DOM 테스트용(`jsdom`, `happy-dom`, `@testing-library/*`)을 추가하지 마십시오. 화면
테스트는 순수 로직 단위 테스트와 사람이 보는 확인 두 층으로 나눕니다.

## CSS

`docs/css-style.md` 를 읽으십시오. 색과 치수는 `src/assets/ui.css` 의 토큰이 유일한
출처이고 `src/ui/theme.test.ts` 와 `src/ui/scale.test.ts` 가 그것을 검사합니다.

</conventions>

<verification>

## 검사

```bash
npx tsc --noEmit && npm test
```

테스트 환경은 `environment: 'node'` 이고 `include` 는 `src/**/*.test.ts` 입니다. DOM 이 없고
`.tsx` 테스트는 수집되지 않습니다. **이 설정을 바꾸지 마십시오.** 화면 렌더링 테스트를
만들려고 시간을 쓰지 마십시오.

## 테스트를 쓸 때

**기댓값을 구현과 같은 식으로 계산하지 마십시오.** 그러면 구현이 무엇을 하든 통과합니다.
숫자를 손으로 계산해 못박으십시오.

<example>
나쁨: `expect(gap).toBeCloseTo(twoLineGap(200, fontSize, subSize))`
왜: 구현이 틀려도 테스트가 같이 틀립니다. 실제로 이 형태의 테스트가 결함을 놓쳤습니다.
좋음: `expect(gap).toBeCloseTo(37.5)` 와 그 숫자가 어디서 나왔는지 적은 주석
</example>

**앵커만 보는 검사를 조심하십시오.** 글자의 `y` 는 중심선입니다. 중심선이 사진 아래에 있어도
글자 윗부분은 사진을 덮을 수 있습니다. 실제로 그 구멍으로 결함이 통과했습니다.

**새로 쓴 회귀 테스트는 옛 코드에서 실제로 실패하는지 확인하십시오.** 확인하지 않으면 그
테스트가 아무것도 붙잡지 못합니다. 임시로 되돌려 돌려 보고 결과를 보고하십시오.

## 사람이 봐야 하는 것

브라우저에서 직접 확인해야만 잡히는 결함이 있습니다. 실제로 이렇게 잡은 것들입니다.

- 세로 사진에서 아래쪽에 안 칠해진 띠가 남던 문제
- `원본` 으로 내보냈는데 사진이 1픽셀 줄어 다시 표본이 잡히던 문제
- 미리보기 배율이 사진 크기를 따라가지 않던 문제

**함정 하나.** 브라우저 패널이 숨겨져 있으면 `requestAnimationFrame` 이 아예 돌지 않습니다.
미리보기는 rAF 로 그리므로 캔버스가 기본 300x150 에 투명한 채로 남습니다. 앱 결함이 아니라
검증 환경 문제입니다. 페이지 쪽 `rAF` 를 즉시 실행으로 바꿔 확인하십시오.

확인하지 못한 것은 **확인하지 못했다고 적으십시오.** 확인했다고 쓰는 것이 가장 나쁩니다.

</verification>

<safety>

## 되돌리기 어려운 동작

파일을 고치고 테스트를 돌리는 것 같은 지역적이고 되돌릴 수 있는 동작은 마음껏 하십시오.
되돌리기 어렵거나 바깥에 보이는 동작은 먼저 물어보십시오.

확인이 필요한 것들입니다.

- 파일이나 브랜치 삭제, `rm -rf`
- `git push --force`, `git reset --hard`, 이미 올린 커밋 고치기
- 원격에 푸시하기, 저장소 만들기, PR 이나 이슈에 글 남기기
- 배포를 실제로 실행하기
- 공개 범위를 바꾸기. Private 을 Public 으로 여는 것은 되돌릴 수 없습니다

막혔을 때 파괴적인 동작을 지름길로 쓰지 마십시오. `--no-verify` 로 검사를 건너뛰거나, 모르는
파일을 지우지 마십시오. 그 파일이 다른 작업자가 작업 중인 것일 수 있습니다.

## 비밀 값

저장소에 API 토큰, 계정 식별자, 분석 도구의 beacon 토큰을 넣지 마십시오. 공개 배포에
민감정보를 두지 않는 것이 이 프로젝트의 목표입니다.

배포는 Cloudflare Pages 의 Git 연동을 씁니다. GitHub Actions 는 검사만 하고 배포하지
않습니다. 그래서 Cloudflare 토큰을 GitHub 에 넣을 일이 없습니다. 이 구조를 바꾸지 마십시오.

</safety>

<working_style>

## 과하게 만들지 마십시오

요청받은 것과 분명히 필요한 것만 고치십시오.

- **범위.** 버그 고치기가 주변 코드 정리를 부르지 않습니다. 간단한 기능이 설정 가능성을
  요구하지 않습니다.
- **주석.** 고치지 않은 코드에 주석이나 타입 주석을 새로 달지 마십시오. 논리가 저절로
  드러나지 않는 자리에만 답니다.
- **방어 코드.** 일어날 수 없는 상황에 대한 오류 처리를 넣지 마십시오. 경계(사용자 입력,
  외부 API)에서만 검증합니다.
- **추상화.** 한 번 쓰는 것에 헬퍼를 만들지 마십시오. 없는 미래 요구를 위해 설계하지
  마십시오.

다만 **같은 계산이 두 곳에 복제되는 것은 다른 문제입니다.** 이 저장소에서 두 줄 간격
계산이 두 파일에 복제되어 있었고, 실제로 양쪽을 나란히 고쳐야 하는 일이 두 번 일어난 뒤에야
공유 함수로 뽑았습니다. 복제가 이미 한 번 표류했다면 뽑으십시오.

## 테스트에 맞추지 마십시오

모든 유효한 입력에서 동작하는 구현을 쓰십시오. 테스트 입력에만 맞는 값을 박아 넣거나
우회하지 마십시오. 테스트는 정확성을 확인하는 것이지 해답을 정하는 것이 아닙니다.

과제가 불합리하거나 테스트가 틀렸다고 판단되면 우회하지 말고 **말하십시오.**

## 열어 보지 않은 코드를 추측하지 마십시오

사용자가 특정 파일을 언급하면 답하기 전에 그 파일을 여십시오. 코드베이스에 대해 주장하기
전에 관련 파일을 읽으십시오. 확신이 없으면 확인하고 답하십시오.

리뷰 결과도 마찬가지입니다. 다른 에이전트의 보고를 그대로 믿지 말고 근거를 확인하십시오.
이 저장소에서 리뷰가 옳았던 적도, 틀렸던 적도 있습니다.

## 임시 파일

작업용 임시 파일이나 스크립트를 만들었으면 끝에 지우십시오. 프로젝트 디렉터리가 아니라
세션 스크래치패드를 쓰십시오.

</working_style>

<known_limits>

## 알고 있으면서 아직 안 고친 것들

새로 발견한 것처럼 보고하지 마십시오. 이미 판단을 거쳤습니다.

- **옵션끼리 얽힌 제약을 막지 못합니다.** 각 값에 `min`/`max` 는 있지만 서로의 관계는
  검사하지 않습니다. `PAD_BOTTOM` 100 에 `SUB_SCALE` 3 처럼 각각은 정상인데 함께 놓으면 글이
  여백을 넘습니다. 한계 자체는 주석과 테스트에 숫자로 못박아 두었습니다. 설정 화면에서 얽힌
  가드를 넣는 것이 제대로 된 해결입니다.
- **`BAR_HEIGHT` 를 아주 낮추고 꼬리 줄을 켜면** 슬롯이 글자 한 줄보다 좁아져 간격을 0 으로
  좁혀도 글자가 사진을 조금 덮습니다. 간격으로는 못 고칩니다.
- **`probeCanvasLimit` 이 첫 내보내기 때 메인 스레드를 잠깐 멈춥니다.** `OffscreenCanvas` 만
  쓰므로 워커로 옮길 수 있는데, `localStorage` 캐시 때문에 메인 스레드에 있습니다.
- **인코딩 실패가 `error.canvas` 로 갑니다.** 그리기는 멀쩡하고 저장 형식만 실패한 상황인데
  더 심각한 진단이 나갑니다. `error.encoding` 키가 필요합니다.
- **사전 완전성 검사가 편방향입니다.** 사전에만 있고 아무도 안 쓰는 키를 잡지 못합니다.
- **`t(labelKey as MessageKey)` 단언이 존재 확인 없이 쓰입니다.** 안전망이 런타임 테스트
  하나뿐입니다.
- **포스터 프리셋의 가운데 줄은 `step` 과 무관하게 언제나 중앙에 놓입니다.** 그 줄의 반높이가
  여백 절반을 넘으면 간격을 아무리 줄여도 여백을 넘습니다.

</known_limits>
